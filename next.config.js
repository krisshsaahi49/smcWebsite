/** @type {import('next').NextConfig} */
const nextConfig = {
	reactStrictMode: true,
	images: {
		domains: ["img.icons8.com","v5.airtableusercontent.com","baserow-media.ams3.digitaloceanspaces.com"],
	},
};

module.exports = nextConfig;
